═══════════════════════════════════════════════════════════
  EASIEST DEPLOYMENT - RAILWAY.APP
  (Database + App Together - No Separate Setup!)
═══════════════════════════════════════════════════════════

🚀 RAILWAY - ONE-CLICK DEPLOYMENT

Railway gives you BOTH database and app automatically!

STEP 1: Go to Railway
→ Visit: https://railway.app
→ Click "Start a New Project"
→ Sign in with GitHub

STEP 2: Deploy from GitHub
→ Click "Deploy from GitHub repo"
→ Select: ssenkula-rivan/kenmark-system
→ Click "Deploy Now"

STEP 3: Add Database (Automatic)
→ Click "New" → "Database" → "Add PostgreSQL"
→ Railway automatically connects it!
→ No manual configuration needed!

STEP 4: Add Environment Variables
→ Click on your service
→ Go to "Variables" tab
→ Add these:

NODE_ENV=production
PORT=5000
JWT_SECRET=764c6004fcf759301a47dd7fb08abb12bfc0cfd1177a6418094e238e08e4aa62eda709b7dcc3cfbdea1bdaad589ff852ff7225c1279b59a45136e8b006128f37
JWT_EXPIRES_IN=7d
CORS_ORIGIN=*
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=1000
LOG_LEVEL=info

(DATABASE_URL is added automatically by Railway!)

STEP 5: Run Migrations
→ Click on your service
→ Go to "Settings" → "Deploy"
→ After first deploy, open "Shell"
→ Run: npm run migrate
→ Run: npm run seed

STEP 6: Access Your App
→ Click "Settings" → "Networking"
→ Click "Generate Domain"
→ Your app is live!

═══════════════════════════════════════════════════════════
  ALTERNATIVE: RENDER WITH BLUEPRINT
═══════════════════════════════════════════════════════════

This uses the render.yaml file to create everything automatically!

STEP 1: Go to Render
→ Visit: https://render.com
→ Sign in with GitHub

STEP 2: New Blueprint
→ Click "New +" → "Blueprint"
→ Connect: ssenkula-rivan/kenmark-system
→ Render reads render.yaml automatically

STEP 3: Review & Deploy
→ Render shows what it will create:
  - PostgreSQL database
  - Web service
  - All connections
→ Click "Apply"

STEP 4: Add JWT Secret
→ Go to your web service
→ Click "Environment"
→ Find JWT_SECRET
→ Replace with: 764c6004fcf759301a47dd7fb08abb12bfc0cfd1177a6418094e238e08e4aa62eda709b7dcc3cfbdea1bdaad589ff852ff7225c1279b59a45136e8b006128f37

STEP 5: Run Migrations
→ After deploy completes
→ Go to web service → "Shell"
→ Run: npm run migrate
→ Run: npm run seed

STEP 6: Access Your App
→ Your URL: https://kenmark-backend.onrender.com
→ Login: admin / admin123

═══════════════════════════════════════════════════════════
  WHICH ONE TO CHOOSE?
═══════════════════════════════════════════════════════════

🏆 RAILWAY (RECOMMENDED)
✅ Easier setup
✅ Faster deployment
✅ Better free tier ($5/month credit)
✅ Database + App in one place
✅ No separate database setup needed

⚡ RENDER WITH BLUEPRINT
✅ More reliable
✅ Better for production
✅ Free forever (with limitations)
✅ Uses render.yaml (already configured)
❌ Slightly more complex

═══════════════════════════════════════════════════════════
  FREE TIER COMPARISON
═══════════════════════════════════════════════════════════

RAILWAY:
- $5 credit/month (usually enough)
- 500 hours execution
- 1GB RAM
- Database included

RENDER:
- 750 hours/month
- Spins down after 15 min
- 512MB RAM
- Database separate but free

═══════════════════════════════════════════════════════════

RECOMMENDATION: Try Railway first - it's simpler!
If you run out of credits, switch to Render.

═══════════════════════════════════════════════════════════
