# API Configuration
# For local development, use /api (proxied through Vite)
VITE_API_BASE_URL=/api

# Network Access Notes:
# - The system is configured to work on local network
# - Frontend runs on 0.0.0.0:3000 (accessible from any computer)
# - Backend runs on localhost:5000
# - Other computers access via: http://YOUR_SERVER_IP:3000
# - Example: http://192.168.1.100:3000
# 
# To find your server IP on macOS:
#   ifconfig | grep "inet " | grep -v 127.0.0.1
#   or
#   ipconfig getifaddr en0
